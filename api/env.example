# Configuración del servidor
PORT=4000
NODE_ENV=development

# Configuración de la base de datos MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password_mysql
DB_NAME=medapp
DB_PORT=3306

# JWT Secret (genera una cadena larga y segura)
JWT_SECRET=tu_jwt_secret_muy_largo_y_seguro_aqui

# Configuración HTTPS local (opcional)
HTTPS_KEY=./certs/key.pem
HTTPS_CERT=./certs/cert.pem

# CORS (en desarrollo puedes usar *, en producción especifica tu dominio)
CORS_ORIGIN=*

# Configuración de email (para OTP y notificaciones)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_app_password_gmail
EMAIL_FROM=tu_email@gmail.com
